export const enum Level {
  Low,
  High,
}

export const enum Direction {
  In,
  Out,
}

export const enum PullUpDown {
  Up,
  Down,
  None,
}

export const enum GpioName {
  RECEIVER_AR20,
  RECEIVER_SERIAL,
  RECEIVER_SERIAL_CLK,
}